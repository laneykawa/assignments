var alpha = "abcdefghijklmnopqrstuvwyz"

// create a key
// create a function to loop through key to shift the letters
// match letters to key
// print results

var caesarCipher = function (str, shift){
    for (var i = 0; i < str.length; i++){
        var stringMatch =
        }
    }
}

// caesarCipher("abc", 1)

// //how to make a key? 

var key = function(shift){
    for (var i = 0; i < alpha.length; i++){
        console.log(alpha[i+shift])
    }
}

key(5);
